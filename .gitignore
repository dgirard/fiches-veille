# Raw data from article URLs
raw-data/

# Fichiers système macOS
.DS_Store
*.DS_Store

# Répertoires temporaires
temp/

# Fichiers sensibles potentiels
.env*
*.pem
*.key
*credentials*
*.p12
*.pfx

# Répertoires de travail
copywriting/
docs/deep research/

# Design system propriétaire — actif éditorial à ne pas publier sur GitHub public
_design-system/

# Briefs et plans brainstorm — peuvent référencer des consultations confidentielles (NDA-sensitive)
docs/brainstorms/
docs/plans/
docs/specs/

# Configuration Claude Code locale (session settings, agent state)
.claude/

# Python build artifacts (post-review pass 2 — ajout des tests scripts/tests/)
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/

# Étage Gold du médaillon — livrables générés (HTML, pptx) + prompts de génération.
# Non versionné : dépôt public, et les HTML inlinent le CSS du design system
# Sharp Artisan (_design-system/, actif éditorial déjà exclu ci-dessus).
gold/
